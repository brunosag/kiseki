# Kiseki Backend

FastAPI and PyTorch backend for the Kiseki dashboard.

Run the API locally:

```powershell
uv run fastapi dev main.py
```

Run checks:

```powershell
uv run pytest
uv run ruff check .
```

