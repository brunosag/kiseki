[project]
name = "kiseki"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "evotorch>=0.6.1",
    "torch>=2.12.0",
    "torchvision>=0.27.0",
    "tqdm>=4.68.1",
]

[tool.ruff.format]
quote-style = "single"
